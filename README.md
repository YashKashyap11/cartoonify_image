# Cartoonify Image using OpenCV (Python)

This is a minimal Python script that takes an input image and converts it into a cartoon-style image using **OpenCV** and **NumPy** only — no extra libraries or GUI frameworks.

## 📦 Requirements

Install the required packages:

```bash
pip install opencv-python numpy
